import {useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import {useAuth} from "../context/AuthContext";
export default function Register(){const{register,getHomePath}=useAuth(),nav=useNavigate();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
async function submit(e){e.preventDefault();setError("");setBusy(true);try{const me=await register(email,password);nav(await getHomePath(me))}catch(e){setError(e.response?.data?.detail||"Couldn't create your account.")}finally{setBusy(false)}}
return <div className="min-h-screen bg-[#050505] text-white flex items-center justify-center px-5 py-12 noise"><div className="fixed inset-0 grid-bg pointer-events-none"/><div className="relative w-full max-w-md reveal"><div className="mb-10 text-center"><Link to="/" className="font-display font-bold tracking-tight">AUTO<span className="text-neutral-500">PUBLISHER</span></Link><div className="eyebrow mt-10">NEW WORKSPACE</div><h1 className="mt-3 font-display text-5xl font-bold tracking-tight">Start the pipeline.</h1><p className="mt-3 text-sm text-neutral-500">Create an account and choose your connection method next.</p></div><form onSubmit={submit} className="panel p-7 space-y-5">{error&&<div className="rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-sm text-red-200">{error}</div>}<label className="block"><span className="mb-2 block text-xs uppercase tracking-wider text-neutral-500">Email</span><input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className="block"><span className="mb-2 block text-xs uppercase tracking-wider text-neutral-500">Password</span><input className="field" type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters"/></label><button disabled={busy} className="btn-primary w-full py-3.5 disabled:opacity-50">{busy?"Creating…":"Create workspace →"}</button><p className="text-xs leading-5 text-neutral-600">Normal accounts start without admin privileges. Your connection setup comes next.</p></form><p className="mt-5 text-center text-sm text-neutral-500">Already have an account? <Link to="/login" className="text-white hover:underline">Sign in</Link></p></div></div>}
