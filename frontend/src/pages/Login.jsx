import {useState} from "react"; import {Link,useNavigate} from "react-router-dom"; import {useAuth} from "../context/AuthContext";
export default function Login(){const {login,getHomePath}=useAuth(),nav=useNavigate();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
async function submit(e){e.preventDefault();setError("");setBusy(true);try{const me=await login(email,password);nav(await getHomePath(me))}catch(e){setError(e.response?.data?.detail||"Couldn't sign in. Check your email and password.")}finally{setBusy(false)}}
return <div className="min-h-screen bg-[#050505] text-white grid lg:grid-cols-2 noise"><div className="relative hidden lg:flex items-center justify-center overflow-hidden border-r border-white/10"><div className="absolute inset-0 grid-bg"/><div className="relative max-w-md px-10"><div className="eyebrow">AUTO PUBLISHER</div><h1 className="mt-5 font-display text-6xl font-bold leading-none tracking-[-.05em]">Publish less.<br/><span className="text-neutral-500">Create more.</span></h1><p className="mt-6 text-neutral-400 leading-7">Your Drive content, AI metadata and YouTube publishing — connected into one quiet automation layer.</p><div className="mt-10 grid grid-cols-3 gap-2">{["DRIVE","AI","YOUTUBE"].map((x,i)=><div className="panel-soft p-4" key={x}><span className="text-[10px] text-neutral-600">0{i+1}</span><div className="mt-7 text-xs font-semibold">{x}</div></div>)}</div></div></div><div className="flex items-center justify-center px-5 py-12"><div className="w-full max-w-md reveal"><Link to="/" className="font-display font-bold tracking-tight">AUTO<span className="text-neutral-500">PUBLISHER</span></Link><div className="mt-12"><div className="eyebrow">WELCOME BACK</div><h2 className="mt-3 font-display text-4xl font-bold">Sign in.</h2><p className="mt-2 text-sm text-neutral-500">Enter your workspace credentials to continue.</p></div><form onSubmit={submit} className="panel mt-8 p-6 space-y-5">{error&&<div className="rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-sm text-red-200">{error}</div>}<label className="block"><span className="mb-2 block text-xs uppercase tracking-wider text-neutral-500">Email</span><input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className="block"><span className="mb-2 block text-xs uppercase tracking-wider text-neutral-500">Password</span><input className="field" type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button disabled={busy} className="btn-primary w-full py-3.5 disabled:opacity-50">{busy?"Signing in…":"Enter workspace →"}</button></form><p className="mt-5 text-center text-sm text-neutral-500">New here? <Link to="/register" className="text-white hover:underline">Create an account</Link></p></div></div></div>}
