import {useState} from "react";

const cloud="https://console.cloud.google.com/";
const openrouter="https://openrouter.ai/";

function ExternalButton({href,children}){return <a href={href} target="_blank" rel="noreferrer" className="btn-ghost inline-flex items-center gap-2 px-3 py-2 text-xs">{children} ↗</a>}
function GuideSection({number,title,children}){const [open,setOpen]=useState(number===1);return <div className="rounded-2xl border-white/10 bg-white/[.025] overflow-hidden"><button type="button" onClick={()=>setOpen(!open)} className="flex w-full items-center gap-4 p-4 text-left hover:bg-white/[.04]"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl border-white/15 text-xs font-bold">{String(number).padStart(2,"0")}</span><span className="flex-1 font-semibold">{title}</span><span className="text-neutral-500">{open?"−":"+"}</span></button>{open&&<div className="border-t border-white/8 p-4 pt-3 text-sm leading-6 text-neutral-400">{children}</div>}</div>}
function Step({children}){return <li className="flex gap-3"><span className="mt-1 flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-white text-[10px] font-bold text-black">✓</span><span>{children}</span></li>}

export default function CredentialGuide(){return <div className="mt-5 rounded-2xl border-white/10 bg-black/20 p-4 sm:p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="eyebrow">BEGINNER GUIDE</div><h3 className="mt-1 font-display text-xl font-semibold">How to get these keys</h3><p className="mt-1 text-xs text-neutral-500">Follow the cards below. Never share secret values with anyone.</p></div><div className="rounded-xl border-white/10 px-3 py-2 text-[10px] uppercase tracking-widest text-neutral-500">3 short sections</div></div><div className="mt-5 space-y-2"><GuideSection number={1} title="Google / YouTube credentials"><div className="mb-4 flex-wrap items-center justify-between gap-3"><p>Use Google Cloud Console to create the OAuth client that lets this app connect your YouTube channel.</p><ExternalButton href={cloud}>Open Google Cloud Console</ExternalButton></div><ol className="space-y-3"><Step>Create or select a project in Google Cloud Console.</Step><Step>Open <strong className="text-white">APIs &amp; Services</strong> → <strong className="text-white">Library</strong>, search for <strong className="text-white">YouTube Data API v3</strong>, and click Enable.</Step><Step>Open <strong className="text-white">APIs &amp; Services</strong> → <strong className="text-white">OAuth consent screen</strong>. Choose External, complete the basic app details, and add your Google account as a test user if Google asks.</Step><Step>Go to <strong className="text-white">Credentials</strong> → Create Credentials → OAuth client ID → Web application. Add the exact redirect URI supplied by your deployment (the default local URI is shown in the project README).</Step><Step>Copy the OAuth <strong className="text-white">Client ID</strong> and its <strong className="text-white">Client Secret</strong> into the two matching Google fields in this Setup form. Do not paste a YouTube API key: this app uses OAuth to authorize channel access.</Step></ol></GuideSection><GuideSection number={2} title="Google Drive service account"><div className="mb-4 flex-wrap items-center justify-between gap-3"><p>Drive is read by the app's backend using a service-account JSON file, not a personal Drive password.</p><ExternalButton href={cloud}>Open Google Cloud Console</ExternalButton></div><ol className="space-y-3"><Step>Select the same project, open APIs &amp; Services → Library, search for <strong className="text-white">Google Drive API</strong>, and click Enable.</Step><Step>Open IAM &amp; Admin → Service Accounts, create a service account, open it, then Keys → Add key → Create new key → JSON. Download the JSON file and keep it private.</Step><Step>The person running the backend sets that file's path as <strong className="text-white">GOOGLE_SERVICE_ACCOUNT_JSON_PATH</strong>. This is server configuration; there is no Drive secret field in this Setup form.</Step><Step>Copy the service account email from Google Cloud Console. In Google Drive, share each source folder with that email as <strong className="text-white">Viewer</strong>. Then the admin can add the folder link under Drive Sources.</Step></ol></GuideSection><GuideSection number={3} title="OpenRouter API key"><div className="mb-4 flex-wrap items-center justify-between gap-3"><p>OpenRouter supplies the key used for AI video metadata.</p><ExternalButton href={openrouter}>Open OpenRouter</ExternalButton></div><ol className="space-y-3"><Step>Create or sign in to your OpenRouter account.</Step><Step>Open your profile's <strong className="text-white">API Keys</strong> section and choose Create key.</Step><Step>Copy the new key once and paste it into this form's <strong className="text-white">OpenRouter API Key</strong> field. The application uses its configured model; you do not need to choose a random model.</Step></ol></GuideSection></div><div className="mt-5 rounded-2xl border-white/8 bg-white/[.02] p-4"><div className="eyebrow">FINAL CHECK</div><ul className="mt-3 grid gap-2 text-xs text-neutral-300 sm:grid-cols-2"><li>✓ YouTube API enabled</li><li>✓ Google credentials configured</li><li>✓ Google Drive configured</li><li>✓ Drive folder access configured</li><li>✓ OpenRouter API key added</li></ul><p className="mt-4 text-xs leading-5 text-neutral-500">Once everything is configured, save your credentials and continue to the dashboard.</p></div></div>}
